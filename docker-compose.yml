services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: foos
      POSTGRES_PASSWORD: foos
      POSTGRES_DB: foos
    ports:
      - "5432:5432"
    volumes:
      - foos_pg:/var/lib/postgresql/data

volumes:
  foos_pg:
